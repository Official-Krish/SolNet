{
  "name": "@axion/ws-relayer",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./index.ts --outdir ./dist --target bun --minify --external cpu-features --external ssh2",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/ssh2": "^1.15.5",
    "jsonwebtoken": "^9.0.2",
    "ssh2": "^1.16.0"
  }
}