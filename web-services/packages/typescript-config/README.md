# `@repo/typescript-config` — Shared TypeScript Configs

Base TypeScript compiler configurations shared across the monorepo.

## Configs

| File | Target |
|------|--------|
| `base.json` | Base config: ES2022, strict mode, bundler module resolution |
| `nextjs.json` | Next.js-specific overrides |
| `react-library.json` | React library overrides (JSX retain) |
